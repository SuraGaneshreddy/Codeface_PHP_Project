Options -Indexes

<IfModule mod_headers.c>
  # SSE endpoints must never be buffered by proxies/modules
  <FilesMatch "\.php$">
    Header set X-Content-Type-Options "nosniff"
  </FilesMatch>
</IfModule>
